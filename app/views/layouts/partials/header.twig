<nav class="layout-navigation">
	<p class="layout-navigation__main">
		{{ HTML.link('about', 'About') }}
		{{ HTML.linkAction('PackagesController@index', 'Packages') }}
		{{ HTML.linkAction('MaintainersController@index', 'Maintainers') }}
	</p>

	{% if auth_guest() %}
		{{ HTML.decode(HTML.link("https://github.com/login/oauth/authorize?client_id=#{Config.get('registry.api.github.id')}", '<i class="icon-github"></i> Login with Github')) }}
	{% else %}
		{{ HTML.linkRoute('maintainer', "Logged in as #{auth_user().name}", auth_user().slug) }}
		{{ HTML.linkAction('MaintainersController@logout', 'Logout') }}
	{% endif %}
</nav>

<a href="{{ url('/') }}">
	<header class="layout-header">
		<h1>Laravel Packages <strong>Registry</strong></h1>
	</header>
</a>
