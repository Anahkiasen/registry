<?php

//////////////////////////////////////////////////////////////////////
/////////////////////////// LOCAL PROVIDERS //////////////////////////
//////////////////////////////////////////////////////////////////////

$providers = array(
	// 'Juy\Profiler\Providers\ProfilerServiceProvider',
);

// Register and boot providers
foreach ($providers as $provider) {
	$provider = App::register($provider);
	$provider->boot();
}
